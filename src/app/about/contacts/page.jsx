import styles from "../../page.module.css";

const Contacts = () => {
    return (
      <main className={styles.main}>
        <h1>Contacts</h1>
      </main>
    );
  };
  
  export default Contacts;